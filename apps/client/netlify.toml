[build]
  command = "npm run build"
  publish = "dist"

# Environment variables (set your backend URL after deploying)
[build.environment]
  VITE_API_URL = "https://your-backend-url.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
